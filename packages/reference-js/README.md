# Reference JavaScript

零依赖 OLL 探索参考实现，当前负责：

- 检查 Authoring Profile 的基本结构和局部引用；
- 将 Authoring Profile 确定性规范化为 Canonical Events；
- 生成可比较的 Semantic BoardState；
- 为完整课程示例提供独立测试。

这不是生产 Runtime，也不处理 DOM、动画、TTS 和像素布局。
