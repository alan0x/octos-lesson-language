[package]
name = "oll-runtime"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["float_roundtrip", "preserve_order"] }
regex = "=1.11.3"

[lib]
crate-type = ["rlib", "cdylib"]
