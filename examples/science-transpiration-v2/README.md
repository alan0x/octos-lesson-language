# Science transpiration V2 teaching-comprehensible slice

This is the third positive teaching-playback specimen and the first one driven
by a real controlled image asset. It separates direct observation, comparison,
inference and scientific mechanism before returning to the source image.

The Authoring lesson references only `asset_id` and authorized `source_region`
values. File URL and normalized overlay bounds are supplied by the Harness host
asset resolver, not embedded in OLL.
