# 交互语义白板 MVP：实施合同

状态：实施中
日期：2026-08-12

这份文档只回答三件事：第一版要做什么，代码放在哪里，怎样算完成。详细语言字段仍由 `SPEC.md` 和 Schema 定义。

## 1. 第一版要解决的问题

现在的 OLL 可以生成静态 Geometry 和 Plot，但不能表达“同一个角度变化时，单位圆上的点和正弦曲线上的点一起变化”。学生也不能直接在白板上留下自己的内容并基于选区继续提问。

MVP 按以下顺序补齐能力：

1. 课程变量和安全数学关系；
2. 2D 自动动画与拖动控制；
3. 学生笔迹的保存、恢复和选择；
4. 选区工具栏与“问小章鱼”；
5. 可检查的互动任务；
6. 可旋转的基础 3D；
7. 恢复、降级、可访问性和性能门禁。

后一个阶段建立在前一个阶段的公开合同上。笔迹、3D 或任务不会反过来发明另一套状态系统。

## 2. 代码和责任放在哪里

- OLL Core：Authoring/Canonical 数据、Validator、Normalizer、Reference Reducer、Semantic BoardState 和 headless conformance tests。
- OLL Web Runtime：Geometry、Plot、动画、交互控制、2D/3D 渲染和浏览器级测试。
- 可选 Ink Runtime：笔迹采集、SVG 序列化、恢复、命中测试和选择适配。它属于 OLL 仓库，但不属于无 DOM 的 Core，也不进入首屏必需包。
- `octos-web`：`/learn` 产品页面、用户会话、API、权限、保存时机和产品 UX。它安装 OLL Runtime，不复制 Runtime 实现。
- Octos 后端：传输、模型调用、资源和持久化；不解释白板动作。

MVP 不做通用插件体系。未来确实需要学科计算组件时，再引入受控组件注册表：组件有稳定 ID、版本、输入输出 Schema、能力声明和宿主批准，不能从 Lesson 中加载任意代码。

## 3. 三份数据分别保存

不能把老师课程、学生原稿和识别猜测混成一个文档。

1. Canonical OLL 日志与 Snapshot 保存老师生成的课程、当前变量值和正式增强结果。
2. 学生输入资源保存原始 stroke 数据及其 SVG 表示；中文、英文和手绘图形使用同一种笔迹格式，识别器只给它们附加不同的候选解释。
3. 来源与确认记录保存“哪个选区产生了哪个建议、学生是否确认”。未确认识别不能改写学生原稿，也不能成为正式课程事实。

## 4. “问小章鱼”具体发生什么

1. 学生框选自己的笔迹或一个课程节点，点击“问小章鱼”。
2. 宿主创建不可变的选区引用，包含资源 ID、选区范围、内容类型候选和当前课程上下文；学生可以补一句问题。
3. 后端把引用和问题交给 learning-coach。原始笔迹只通过受控资源传递，不塞入 OLL 动作。
4. 模型返回新的 Authoring Lesson/Step，经过 Schema、语义校验和 Normalizer 后才播放。
5. Runtime 把回答、批注、函数图或辅助图放在选区旁边，并用来源关系连接；被选中的原内容保持不变。
6. 识别不确定、模型失败或 Runtime 不支持所需能力时，界面给出明确状态和重试入口，不伪造成功。

## 5. 四个贯穿验收场景

### A. 单位圆与正弦波

一个 `theta` 同时驱动单位圆点 `(cos(theta), sin(theta))`、投影、角弧和正弦图活动点 `(theta, sin(theta))`。自动播放和学生拖动都修改这个变量。0、π/2、π、3π/2、2π 的几何与函数值必须一致；刷新后保持最后角度。

### B. 学生写函数并生成图像

学生写下 `y=x²-2x-3`，原始笔迹保存且永不被替换。学生框选后看到“生成函数图像”等候选动作；确认后出现独立 Plot 节点，并能追溯到原选区。识别错误时可以修正候选，不污染原稿。

### C. 物理互动任务

学生通过受限控件改变力或速度，图示和数据读数由同一变量更新。任务有明确目标、检查条件和反馈。错误操作得到针对性提示，而不是只显示“对/错”。

### D. 可旋转基础 3D

学生可以旋转、缩放立方体或基础函数曲面；相机变化不改变对象的数学语义。没有 WebGL 或开启降低动态效果时，Runtime 显示受控静态降级，而不是空白卡片。

## 6. 当前实施结果

第 1 阶段已经完成：数值变量、安全表达式、确定性重算和恢复状态已进入 Core。

第 2 阶段也已经完成 OLL 仓库内的实现：Authoring 可以声明变量动画和滑杆；Web Runtime 可以连续更新同一个变量，暂停/刷新后续播，学生拖动滑杆后接管当前值，并在降低动态效果时直接应用终值。单位圆与正弦图是首个垂直样板。

已完成证据：

- Authoring Schema 能声明数值变量；
- Geometry/Plot 可以把受支持的数值字段绑定到受限表达式；
- Validator 拒绝越界变量、未知变量、未知目标和非有限结果；
- Normalizer 生成稳定目标 ID；
- Reference Reducer 把当前变量值保存在 Semantic BoardState，并一次重算所有相关节点；
- 单位圆 fixture 在五个特殊角上通过 headless 断言；
- 自动动画中 Geometry/Plot 始终读取同一个 `theta`；
- 动画暂停恢复、滑杆接管和降低动态效果都有 Web Runtime 测试；
- 现有静态课程和 Runtime 测试不回归。

第 3 阶段已经在 OLL 仓库完成首个正式切片：

- `packages/ink-runtime` 精确依赖 `js-draw 1.33.0`，复用其笔、压感、橡皮、选择和撤销栈；
- Web Runtime 公开当前可见镜头、镜头订阅、双向坐标转换和输入权移交，不再读取私有 CSS；
- 学生原稿以独立 SVG 资源保存，携带格式版本、编辑器版本、递增版本和 SHA-256；
- 刷新恢复会验证资源完整性，损坏时显式失败；
- 选区可以冻结为独立来源快照，不保存编辑器内部组件编号；
- Harness 的主包不包含 `js-draw`，点击“启用书写”后才加载 Ink Runtime；
- OLL reset、seek 和课程动作没有修改学生笔迹的入口。

第 4 阶段的底层合同已经完成：选区快照带格式版本、来源范围和 SHA-256；学生操作日志保存选区引用而不复制或改写原稿。产品宿主可以把选区图片和用户问题发送给 learning-coach，并把返回的独立增强卡片放在原选区旁边。

第 5 阶段已经完成：课程可以声明完成条件、允许的学生操作、提示和反馈。Runtime 只根据已提交的学生操作判断任务，支持提示、重试和刷新恢复。

第 6 阶段已经完成基础切片：`scene3d` 支持立方体、球体、圆柱、圆锥、受限 `z=f(x,y)` 曲面、轴对齐截面以及点、棱、面的坐标高亮；学生可以旋转、缩放、切换等轴/正视/俯视视角和复位。连续手势只保存一次视角变化，刷新后恢复，并可由 `scene3d_view_target` 教学任务按角度和缩放容差判定。每个场景必须带静态说明，交互渲染失败时不会留下空白卡片。三维学科模拟、任意网格、碰撞和插件组件不在本切片范围。

下一阶段是生产验收，而不是继续扩充协议：在真实 `/learn` 链路验证选区提问、3D 生成与交互、刷新恢复、触摸/触控笔、弱网和性能。
